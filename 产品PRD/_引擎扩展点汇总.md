# 引擎扩展点汇总（全 PRD engineFlags 去重总表）

> 本表把 00 / 00b / 01~09 各模块 `engineFlags` 里的 ➕extension / ❌incompatible 去重汇总成**一张表**，按**工作量 / 风险**排序（最大项在前），供乙方排期与甲方风险评估。
> 引擎源：`技术文档/00~09`（核心机制以代码为准）。本系统 = 业务 system-of-record，财务做账在金蝶云星空。
> 决策基线：决策 A（财务行级范围收紧 + 新增 FINANCE_DIRECTOR）、决策 B（多公司单用户绑定、各家角色相同）已落位（见 00 总览）。
> **2026-06-16 甲方反馈增量**：Q7（互检可同一人→取消"复核人≠制单人"validator）、Q15（毛利率两套口径→报表手写路由增 `margin_basis`）、Q18（报价利润点/毛利对 SALES+SA 可见、采购买价对销售端隐藏→`profit_point` 不入隐藏集、采购买价列入 `BUY_PRICE_FIELDS`）；新增扩展点：售后工单 `SERVICE_TICKET`、委外加工（薄）入库类型 + 委外发料出库类型、特批发货独立 doc_type、顺丰集成升级「接好等配置」、标签/单据模板细化为两份规格册。
> **2026-06-16 follow-up 增量**：① **会签标准件 `cosign`**（串行两节点会签 = 全 PRD 通用 pattern：备货 ≥20 万会审、客户认证内审/法务对齐/签署，统一用节点级 allowed_roles 串接规避 D-02e）；② **客户 Forecast 接单**（`customer_forecast` + `forecast_acceptance` 两轻量 doc_type + 联动备货/转 SO effect，**首期手录留痕；直连客户异构系统列后期 ➕**，开关 `CUSTOMER_PORTAL_SYNC_ENABLED` 默认 OFF）；③ 售后工单 `SERVICE_TICKET`（前波已列，归类不变）；④ 顺丰集成（前波「接好等配置」，归类不变）；⑤ **非功能安全整改**（10 非功能需求册 = 会话生产化欠账的完整落地清单，与第 9 项同源）。**A-1 金蝶 OpenAPI 接口已确认开放**——第 1 项金蝶推送的「前提阻塞」降级为「真实接入排期」（凭据 + 组织码 + 账套字段编码），扩展工作量不变。
> 日期：2026-06-16。状态：待甲方评审。

---

## 一、排序总表（按工作量 / 风险，最大项在前）

> 列：扩展点 | 涉及模块 | 引擎落点 | 是否阻塞（🚫=阻塞主交付，须先确认前提；⚠️=高风险/需引擎改造；➕=标准扩展，不破底座）

| # | 扩展点 | 涉及模块 | 引擎落点 | 是否阻塞 |
|---|---|---|---|---|
| **1** | **金蝶单据推送 outbox + 适配器**（全 PRD 最大项） | 07b（主）· 01/03/04/05/06 全推单单据 | 新增 `kingdee_outbox` 模型（`__queryable__`）+ 每单据 `@register_transition_effect(auto)` 写 outbox + `@register_command("push_kingdee_*")`（Save→Submit→Audit，按金蝶 OpenAPI 网关形态 `/v2/<域>/<formId>/<操作>` + `{"data":[...]}` + `accesstoken`/`x-acgw-appid`/`Idempotency-Key`）+ HTTP 出站 + 凭据；引擎**无任何金蝶推送能力**（引擎 00 D7、08 §8.8）；含组织映射 company_id→org、基础资料编码对齐、`cas_*` 用 `_id` 换码 | ⚠️ **接口前提已确认（A-1）**：金蝶 OpenAPI（appid 鉴权）开放、Save/Submit/Audit 写入接口齐全→**不再阻塞**；降级为**真实接入排期**（凭据 appid/appSecret + 6 公司组织码 A-7 + 账套固定字段编码 A-5/A-6）；接入前以 dry-run/开关 OFF 验证适配器 |
| **2** | **标签 / 单据模板引擎**（二维码拼接可配 + 渲条码 + 派生字段）**已细化为两份规格册** | 09（主）· **09-标签模板规格(逐客户)** · **09-单据模板规格(PL-INV-送货单)** · 02 标签指针 · 03a 入仓标签 · 03b 客户标签 · 05b 单据模板 | 新增 `label_template`/`doc_template`（doc_type）+ `*_field_line` 子表 + 二维码拼接 command（逐客户分隔符 `& ; + , _`×字段序）+ 派生字段表达式 + **旭创/智禾数量列+外箱发货日期渲条码** + custom_html 渲染逃生舱；引擎仅 `custom_html` innerHTML 逃生舱（引擎 08 §8.10，⚠️ XSS 须后端白名单拼装管控） | ⚠️ XSS 管控 + ~21 客户字段来源逐家签字（gap C-1/C-2/C-8）；规格册已落字段清单/客户差异矩阵 |
| **3** | **编号规则（月度重置 + 连号 + 抬头码）** | 09（主）· 01~06 全单据 `*_number` | 新增 `numbering_rule` 表（公司×单据×前缀×重置周期×current_seq×current_period）+ 取号 effect/command（**原子自增 + 跨期重置**，单事务行锁防撞号）；引擎 `_auto_fill_required_fields` 仅给唯一前缀，不支持月度重置连号（引擎 02 §2.3） | ⚠️ 连号=金蝶幂等键，并发撞号风险须行锁；改 `load_commands()` import（D-03a） |
| **4** | **多公司单用户切换（决策 B：用户×公司授权，各家角色相同）** | 01（主，EXT-01-B）· 00b 工作台/审批/通知 · 全模块行级隔离 | 接线死代码 `UserCompanyAccess`（`_company_filter` 从不读它，D-05c）+ 加 `is_primary`/`valid_until` 列（**无需 per-company 角色列**，决策 B）+ `_company_filter` 改读"已开通公司集 ∩ active_company_id"（read-privileged 看全部）+ 会话载 `active_company_id`（重签 Cookie 或带请求头）+ 前端公司切换器 | ⚠️ 会话契约层改动（GAP D-06，"是否做"已由决策 B 拍板，仅余实现选型） |
| **5** | **报表 / 看板 / 跨公司汇总（非元数据化）** | 08（主）· 00b 工作台看板卡 · 04a PO 总表跨表聚合 · 05b SO 签单大表全链拼装 · 07a 应收应付提醒 | 手写报表路由（`routers/reports.py` 手写 SQL）+ `_company_filter` 租户作用域 + `/api/aggregate` group_by；**报表层字段防火墙须手写路由自裁剪毛利/成本列**（引擎自动遮蔽不覆盖手写聚合 JSON，EXT-08-B）；引擎报表非元数据化（引擎 00 D7） | ⚠️ 手写聚合不自动遮蔽防火墙，须逐路由按角色裁剪（防销售反推成本） |
| **6** | **扫码枪 / OCR 采集** | 03a 入库（顺序锁/二维码切换）· 03b 出库/盘点扫码 · 全模块 SN/LOT/入仓编号 | 前端输入增强填字段（网格聚焦 + 扫码顺序锁 + 供应商二维码切换 + 错序清行 + OCR 拍照回填人工复核）；引擎无采集层（蓝图 §1.3、访谈 03）；**扫码字段一律手填兜底**（硬 UX 公约） | ➕（甲方已买贵 OCR 大机，本系统轻量前端 OCR 即可） |
| **7** | **物流 API（顺丰）货物进度（「接好等配置」build-and-wait）** | 06-5（主，⑪）· 03/05 运单进度展示 | 新增 `shipment_tracking(+_node)` 表 + 顺丰适配器命令 `sf_query_tracking`/`sf_subscribe_tracking` + 入站 webhook 路由 + 前端进度时间轴 + 运单号联动；**集成全部成形可编译运行**，仅鉴权凭据（appKey/checkWord/customerCode）+ base_url 留空占位，开关 `SF_INTEGRATION_ENABLED` 默认 OFF；引擎无任何外部 HTTP/物流 API（核对 command_registry 无 httpx/requests） | ⚠️/接好等配置 前提=顺丰 API 使用权购入即填凭据开开关（甲方愿买未买，gap A-12）；配置未到=手填兜底不阻塞 |
| **7b** | **客户 Forecast 接单（05d，升格）/ 直连客户供应商系统（后期）** | **05d**（主，原 05b-7 占位升格）· 04b 备货联动 · 05b 转 SO | **首期=轻量手录留痕**：两轻量 doc_type `customer_forecast`+`forecast_acceptance` + 联动备货/转 SO effect（明细见客户销售域表）；**直连客户异构系统=后期 ➕**（开关 `CUSTOMER_PORTAL_SYNC_ENABLED` 默认 OFF，不直连烽火多系统等） | ➕首期手录（已落地）/ 后期占位直连（决策⑫范围待定 G-1） |
| **8** | **边级角色推进路径补齐（FR-2.7，引擎自身欠账）** | 00b（EXT-00b-D）· 01/03/04/05/06/09 凡依赖边级角色的关卡 | 引擎 02 D-02e：`execute_transition` 推进路径当前**只校验节点级 `allowed_roles`**，边级 `next[].roles` 仅 preview/枚举强制 → 补齐推进路径边级 roles 校验；**各模块缓解=优先用节点级 allowed_roles 落关卡 + 审批只经 commit_card（前置 preview 已校验），禁止裸调 execute_transition** | ⚠️ 引擎自身欠账，未补齐前靠纪律缓解 |
| **9** | **会话安全生产化（引擎自身欠账）+ 非功能安全整改（10 非功能需求册）** | 01（EXT-01-K）· **10 非功能需求**（整改清单母册）· 全系统登录态 | 引擎 05 D-05e/f：dev `secret_key`、`CORS allow_origins=['*']+credentials`、口令 SHA-256+全局静态盐（`"photonteck_salt"`）→ 换 secret、收紧 CORS、口令换 bcrypt+每用户盐、按需服务端会话吊销（当前签名 Cookie 无吊销=非"实时踢人"）；**10 非功能需求册**把这些整改 + 性能/可用性/审计留存/备份/合规收口为上线门槛清单 | ⚠️ 上生产前必整改；决定是否支持"立即踢人"（gap D-2）|

---

## 二、其余 ➕extension（标准扩展，不破底座；按域归类）

### 权限 / 账号（01）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 角色键对齐（含决策 A 新增 `FINANCE_DIRECTOR`） | 代码角色键（`SALES_ASSISTANT/.../OPERATIONS/ADMIN`）与总览角色不一一对应（缺纯 SALES/FAE/LOGISTICS_LEAD/FINANCE_DIRECTOR，多 OPERATIONS）→ 对齐枚举 + 补 `ROLE_ALLOWED_TABLES`/`_can_view_*`；**`FINANCE_DIRECTOR` 并入行级只读 privileged、`FINANCE` 移出**（决策 A） | ➕ |
| privileged 成员配置化 + **FINANCE 移出行级 privileged**（决策 A） | `_company_filter` 现硬编 `("BOSS","FINANCE")` → 改 `("BOSS","FINANCE_DIRECTOR")`；表级全权 `FULL_ACCESS_ROLES` 与行级两集不同（D-05g），不要假设一致 | ➕ |
| 会话载 `active_company_id`（决策 B 已定可实现，原 ❌ 升级） | 会话 Cookie 加 `active_company_id`：重签或带请求头（仅余技术选型 GAP D-06） | ➕ |
| 利润点入字段防火墙（漏点必补） | `profit_point` 当前**未被** `BUY/SELL_PRICE_FIELDS` 覆盖 → 纳入隐藏列、对 SALES 不可见（蓝图 §3.2） | ➕（漏点必补） |
| 细粒度数据范围（本人/本部门/本产线） | 现行级仅"本公司 vs 全部"两档 → 加 `data_scope` + `created_by_id`/`department` 行过滤 | ➕ |
| 用户/权限变更审计（必做） | 现 `_audit` 只覆盖流程定义变更 → 用户 CRUD/角色/授权/防火墙配置变更须写只增审计（蓝图 §1.3） | ➕（必做） |
| 临时代管授权（休假代拉） | `UserCompanyAccess` 加 `valid_until` 软到期（访谈 08:1042） | ➕ |
| 防火墙 / 表级权限配置 DB 化 | `BUY/SELL_TABLES`、`_can_view_*`、`ROLE_ALLOWED_TABLES` 现硬编 → 迁 `role_definition`/配置表 | ➕（长期） |
| 公司区域配置字段 | `Company` 加 `region(HK/内地)`/`invoice_title`/`numbering_prefix`/`kingdee_org_code` | ➕ |
| AgentLog 只读端点 | 引擎 07 D-07c；本项目不做 AI，优先级最低/可不做 | ➕（可不做） |

### 工作台 / 审批 / 通知（00b）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| **通知子系统**（本模块最大） | 新增 `notification` 模型 + 态推进 effect 派发 + **定时扫描**（到期/退运180/样品超期，引擎无定时器须新增调度）+ **邮件适配器**（`@register_command` 幂等键=通知号）+ 站内未读徽标；待办（todo）≠ 通知（事件驱动） | ⚠️ 引擎无原生通知子系统 + 无定时器 + 无邮件 |
| 会审 / 联签（备货 ≥20 万） | 默认**拆串行两节点**（PM 节点→财务节点）用现成 allowed_roles 串接；仅"并签/任意顺序双签"才需引擎联签扩展 | ➕（默认无需改引擎） |
| 角色看板卡（报表脚手架） | 工作台看板卡 = 手写路由 + 租户作用域；KPI 落 6 报表 | ➕ |
| read-privileged 待办污染（讨论） | `list_user_todos` 对 BOSS/admin 绕过看全部，可能把"待审"塞进 BOSS/财务总监待办 → 对 read-privileged 只渲染只读旁观、隐藏写动作（决策 A 一致性，gap B-4） | ⚠️（讨论） |

### 仓储 WMS（03a/03b）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 库存状态枚举扩 7 态 | 扩 `inventory.status`（QUARANTINE/NG/SAMPLE/VENDOR_HOLD/SCRAP）+ 各态可售性校验；现仅 AVAILABLE/RESERVED | ➕ |
| 来源/品质标记 + 原厂报备客户（串货隔离） | `inventory` 加 `source_marker`/`reported_customer_id`；驱动 RMA 好货保留标记/PCN 限售/**串货隔离出库校验**（蓝图 §5.2） | ➕ |
| 入库类型 + 头部 supplier/customer/reviewer + 6 明细补列 | 加列 + 一次 alembic 迁移（remark/customs_fee/freight_fee/import_export_cert/bag_seal_date/ba_hold）；**`inbound_type` 枚举含「委外加工入库」** | ➕ |
| **委外加工（薄）：委外发料 + 委外加工入库（03a-9）** | `inbound_type=委外加工入库`（复用 `GOODS_RECEIPT` doc_type + 子表 + ★PA 审核）+ `outbound_type=委外发料`（复用出库单 doc_type，**不走客户发货财务放行关卡**，对象=委外方非客户）；委外方=供应商主数据；**不建工序/BOM/工时/领料倒冲**（做薄）；推金蝶=委外加工入库单/其他入库单 + 委外出库单/其他出库单 effect | ➕（做薄，甲方 2026-06-16） |
| 互检可同一人复核（Q7）= **取消**「复核人≠制单人」校验 | **不实现** `@register_transition_validator(created_by_id != user)`——仅靠 `review` 态 WorkflowLog 留痕（效率优先，甲方 Q7）；**已从 engineFlags 移出**（原拟职责分离 validator 不做）| —（移出，留痕替代） |
| 审核 PA 自动匹配（供应商→PA） | `supplier_pa_map` 或 `supplier.default_pa_id` + 创建态 effect 带出 reviewer | ➕ |
| 出库扣库存 + 写流水 | AUTO effect（`OUTBOUND_ORDER, shipped`）；新增 wms effect 模块 | ➕ |
| 串货隔离校验（原厂报备客户绑定） | `@register_transition_validator` + 节点 hard_rule | ➕ |
| ~~互检人 ≠ 制单人~~ **（甲方 Q7：互检可同一人，此 validator 不做）** | 原拟 `@register_transition_validator(created_by_id != user)` → **取消**，仅 `review` 态 WorkflowLog 留痕（见上「互检可同一人复核 Q7」行）| —（取消）|
| 每包照片留证 / 出库照片 / 合同附件 / invoice 附件 | 多图引用字段 + 上传端点；引擎无附件/对象存储原生支持（跨 03b/05b/07a 共用扩展） | ➕ |
| 出库日期"27 号后算下月 1 号" | `@register_transition_validator` 按盘点截止日校正 | ➕ |
| 入仓编号 cell 选择器带「本公司+可售+结存>0+串货匹配」过滤 | 扩展 SubTableEditor fk 选择器查询参数（跨 03b/05b 共用） | ➕ |
| 盘点 27 号定时生成 + 扫码计数 + 差异→库存调整单 | 引擎无定时器（默认手动建单）；前端扫码增强；EXPLICIT effect 派生调整单 | ➕ |
| movement_type 枚举补 | STATUS_CHANGE/COUNT_ADJUST 等值集扩展 | ➕ |
| Excel 导入命令 | 批量建单命令 + 行级错误回执；导出复用 DataExplorer CSV | ➕ |

### 采购供应链（04a/04b）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 对原厂询价模型 `supplier_inquiry(+_line)` | 新增 doc_type + 子表 + 流程 + 迁移；引擎只有 CRM 侧 SALES_INQUIRY | ➕ |
| 内部询价单扩 6 列 / PO 头扩列 | `sales_inquiry`/`purchase_order` 加列 + 迁移 | ➕ |
| PO 流程重构（拆 ★FINANCE 采购审批节点；入库/发票移出 PO 大流程） | 改 seed 的 `PURCHASE_ORDER` WorkflowDefinition（现 K3 镜像把全链塞一条流程） | ➕ |
| 产品代码按供应商维度过滤的型号选择器 | cell 选择器加 `supplier_id` 约束查询（一型号多 code，跨 05b 共用） | ➕ |
| PO 总表跨单据聚合视图 + PA 数据范围过滤 | 只读聚合端点（PO+在途+发货+付款沿 FK）+ 行级 PA/产线过滤（含 back-up） | ➕ |
| 采购在途模型 + 货期/到货提醒（调度） | 新增 `purchase_in_transit` + 提醒命令；引擎无在途、无定时器 | ➕ |
| 进项发票独立流程 + ★审核节点 + 形成应付/推金蝶 effect | 新增/重构 `PURCHASE_INVOICE` WorkflowDefinition + effect | ➕ |
| 付款申请货后付款 + 到账确认 + 推金蝶 | 扩 `advance_payment` 加发票FK/due_date/confirmed + effect | ➕ |
| 新增 `STOCK_UP_REQUEST`/`SAMPLE_SDN`/`RMA` 单 + 状态机 + 子表 | 新模型 + 迁移 + WorkflowDefinition；引擎无现成（引擎 02 §2.9 排除备货/样品/退货） | ➕ |
| 备货 ≥20 万多签会审 + 金额阈值分流 + 消单 effect | 串行会签子状态（规避 D-02e）；出边 hard_rules `doc.amount<200000`；SO 成交→累加已消 EXPLICIT effect | ➕ |
| 备货/样品超期天数计算字段 + 提醒 | 派生只读列/前端计算 + 每日定时任务；引擎无原生计算列/定时器 | ➕ |
| RMA SA/PA 双视图（决策⑨）= 字段防火墙 | (表×角色) 列遮蔽，引擎原生 query+schema 两路（✅compatible，无需新机制） | ✅ |
| RMA 核料判定 + 货回入库带来源标记 | hard_rule 跨表 lookup 倒查 + `GOODS_RETURNED` effect 写退货入库 + source_marker（与 03 共用） | ➕ |
| SO 审核 → 采购通知派生 effect | `register_transition_effect`（SALES_ORDER 审核→PURCHASE_NOTICE）；改 `load_commands()` import | ➕ |

### 客户销售（05a/05b/05c/05d）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 报价单成本/利润点字段防火墙（含阶梯子表列） | `quote.cost/profit_point`、`quote_tier_line.cost_unit/unit_profit_point` 对 SALES 删 | ➕ |
| PM「是否报价」门控 + 利润点定价关卡 | 节点 `allowed_roles=[PM]`（规避 D-02e 边级角色） | ➕ |
| 线索转商机 / 报价转 SO 草稿（可选）/ 认证完成回写客户码 派生 effect | `@register_transition_effect`（EXPLICIT）；改 `load_commands()` import | ➕ |
| 报价含税/不含税按税率换算（RMB 13/6/9/1%） | SubTableEditor 字段联动 或 validator/hard_rules；引擎仅自带 qty×price | ➕ |
| 商机漏斗/转化率/无进展超期提醒 | 06 报表手写聚合 + 通知中心规则；引擎无定时器 | ➕ |
| 科研细分市场字典 + 事业部二级字典 | 02 主数据/08 配置枚举字典 | ➕ |
| SO 下采购通知（EXPLICIT 派生）+ ★预付到账闸 + 减 SO 在途 | EXPLICIT effect + validator 查到账确认 + AUTO effect 维护在途派生字段 | ➕ |
| 发货申请必关联 SO + ★财务审核（货不能出仓）+ 发票vs发货一致 | 字段必填 + hard_rule（✅）；`finance_review` 节点 allowed_roles=[FINANCE]（规避 D-02e）；一致性 validator | ✅/➕ |
| 一签宝电子签章集成 | 外部签章状态字段 + 占位集成（首期人工上传盖章 PDF） | ➕/占位 |
| **特批发货（先发后补单，05b 页面 4b）** | 新增独立 doc_type（无 SO 例外）+ 状态机 `draft→★finance_special_approval→approved→shipped_pending_so→reconciled→closed` + ★FINANCE 特批审节点 allowed_roles=[FINANCE] + 强制补录 SO 勾稽 effect（回填 SO 号/抵减在途/补推改推金蝶销售源，幂等键=补单 SO 号/原出库号）+ **可隐藏模块开关** `feature.special_batch_shipment`（per-company 默认 OFF）；与「发货申请必关联 SO」零容忍隔离（独立 doc_type 不污染） | ➕（甲方 2026-06-16 拍板做，默认隐藏）|
| **售后技术工单（05c）** | 新增 doc_type `SERVICE_TICKET` + 一行 WorkflowDefinition（建单→处理→关闭/`ESCALATED_RMA`）+ 可选子表 `service_ticket_line`（多型号/SN）+ 轻量 effect（判定退换→**派生/关联 RMA**，04b）；引擎无售后脚手架（02 §2.9 排除退货/售后）；**不做**派工/工时/备件/SLA 硬卡 | ➕（甲方 2026-06-16 拍板做，轻量）|
| **客户认证 + 会签（05a 页面 7，`VENDOR_QUAL`）** | 新增 doc_type `vendor_qualification` + 协议审查子表 `qual_agreement_line` + **会签状态机**（资料收集→内审→法务/对齐→签署→完成，**用 `cosign` 标准件**：每态节点级 allowed_roles 串接，规避 D-02e）+ 完成回写客户供应商码 effect；协议正文审在系统外（纯文本审，访谈 09:552）| ➕（轻量，会签复用标准件）|
| **会签标准件 `cosign`（全 PRD 通用 pattern）** | **统一会签实现规范**：多人/多角色会签 = **拆串行两/多节点**（每态一个节点级 `allowed_roles`，前态签完推下一态），落在底座现有能力内纯 ✅、**规避 D-02e 边级角色推进路径欠账**；落点：备货 ≥20 万会审（PM→FINANCE，04b）、客户认证内审/法务对齐/签署（05a）；**仅"并签/任意顺序双签"才需引擎联签扩展**（默认不做，待甲方 B-3/B-4）| ➕（规范，默认无需改引擎）|
| **客户 Forecast 接单（05d）** | 新增两轻量 doc_type `customer_forecast`（+ 子表 `customer_forecast_line` 滚动月份网格）+ `forecast_acceptance`（+ 可选子表）+ 各一行 WorkflowDefinition + 联动 effect（预测行→起 04b 备货建议草稿；接单→转 05b SO 草稿，靠 FK `stock_up_request.source_forecast_line_id`/`sales_order.source_acceptance_id`）+ 交叉应答 gap 计算（前端/派生列）；引擎无 Forecast 模型、无外部客户系统集成；**首期手录留痕、不推金蝶（无财务凭证含义）** | ➕（甲方 2026-06-16 占位⑫升格为轻量手录留痕）|
| **Forecast 直连客户供应商系统（后期 ➕）** | **后期扩展、首期不做**：直连客户异构系统（烽火多套 / 各家一套）拉 forecast / 推接单应答 / EDI；新增客户系统适配器 + 入站/出站 + 开关 `CUSTOMER_PORTAL_SYNC_ENABLED` 默认 OFF；客户系统五花八门、各家一套→逐客户适配；**不做**预测算法/准确率 AI（甲方明确不做 AI）| ➕/后期占位（是否做、做哪些客户待甲方 G-1）|

### 报关（06）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 报关单 doc_type `CUSTOMS_DECLARATION` + 商品明细子表 + 状态机 | 新增模型 + 迁移 + WorkflowDefinition；引擎**无任何报关脚手架**（与 03 域"底座已存在"相反，报关全新建） | ➕（工作量集中） |
| 从出入库批次按 PO×型号×HS×原产地自动聚合明细 | `@register_command` 读批次聚合写子表；改 `load_commands()` import | ➕ |
| 合规五件套申报阻断 + 自动派 PA 补录 | `@register_transition_validator`(DECLARED) + 派单 effect；依赖 02 主数据合规栏位 ➕ | ➕ |
| 产品主数据合规五件套栏位（归 02 主数据） | `Material` 加 `hs_code_cn/hs_code_hk/hs_code_origin/cn_name/eccn/is_battery` + 规格书附件；导入 HS 表（缺 33 HS/821 中文名期初补） | ➕ |
| 退运 180 天倒排 + 香港退香港硬校验 + 报关单号回写 | 计算字段 + hard_rule/validator + `origin_declaration_id` 自引用 FK + `RELEASED` effect 回写 | ➕ |
| 报关费分摊回写批次到岸成本 + 金蝶增量推送 | `@register_command` 分摊 + `inventory.landed_cost` 列（与 03 入库 E4 同源）+ outbox 增量 | ➕ |
| 进出口证台账 + 报关行挂证 + 效期预警 | 新模型 `customs_license`（`__queryable__`）+ 行级 FK + 前端效期着色 | ➕ |
| 报关资料清单导出（缺件提示） | `@register_command` 生成纯文本清单 + 缺件标注 | ➕ |

### 主数据（02）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 型号规格书 / 附件存储 | 引擎无原生附件域；新增附件存储或 `product_attachment_line` 子表 + 上传端点 | ➕ |
| 「1 线=1 供应商」唯一约束 | 业务 validator 或 DB 唯一约束；引擎不原生强制业务唯一性 | ➕ |
| 金蝶往来单位 / 物料档主数据同步 | 复用 kingdee_outbox 适配器，新增主数据同步触发；默认随首张业务单带抬头/物料 | ➕（待甲方） |
| 库位平面图可视化选位 | 对接仓库平面图 PDF（占位），默认纯编号下拉 | ➕/占位 |
| 主数据建档审核态机（客户/产品可选） | 若需审核注册轻量 WorkflowDefinition；默认纯 `__queryable__` 无态机 | ➕（轻量） |

### 财务视图 / 单据中心（07a/07b）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| **拉金蝶已确定应收/应付视图**（只读展示，不回填不重算） | 新增只读拉取适配器 + `kingdee_ar_snapshot`/`kingdee_ap_snapshot`（`__queryable__`，带 company_id+时间戳）；与推送相对、方向相反；引擎无金蝶拉取 | 🚫 前提=金蝶能否提供已确认余额视图（gap A-1） |
| 到期/超额/到期付款提醒（定时扫描 + 派发） | 定时扫描 `@register_command` 写通知；引擎无定时任务/通知引擎 | ➕ |
| 预收/预付到账确认推金蝶 + 解锁 P01 | confirmed effect 写 outbox；解锁 P01 用 **SO 边 validator 查 confirmed 到账确认单**（非跨单据写） | ➕ |
| CN 共用 RMA 号（不自增） | `rma_number` 列非自增，创建时不被 `_auto_fill_required_fields` 覆盖 | ➕ |
| 链路追踪业务配置 | `chain.py` 的 GROUPS/STAGE_LABELS/TERMINAL_* + FK 遍历需业务配置；末端应收=拉取视图节点需兼容 | ➕ |
| 主数据编码 ↔ 金蝶基础资料编码对齐 | 2 主数据加「金蝶编码」字段或映射表；`cas_*` 单据 `_id` 换码多一跳 | ⚠️（不统一则维护成本） |
| company_id → 金蝶组织映射 | 8 配置「组织映射」表 | 🚫 前提=6 公司↔金蝶组织清单（gap A-7） |
| outbox↔CommandLog 接线（重推）+ DataExplorer 行内重推动作 | outbox 存 command_log_id 复用 retry；前端壳挂自定义行动作 | ➕ |

### 配置 / 模板（09，已并入上方第 2/3 项主表）

| 扩展点 | 引擎落点 | 是否阻塞 |
|---|---|---|
| 标签派生/计算字段 DSL | 复用 rules.py DSL 或拼装 command 内有限函数集；语法待定 | ➕ |
| 打印驱动集成（BarTender 类） | 渲染产物→物理打印机；集成方式待甲方 | ➕/占位 |
| 审批流真会签（多人全签） | 节点 allowed_roles 为"任一可推进"；多人全签拆串联节点或会签计数 validator | ➕ |
| 提成规则引擎 | `commission_rule` + 计算 command；公式待甲方 | ➕/占位 |
| 审批流配置（唯一 ✅原生） | `WorkflowDefinition.states` + FlowEditor 管理态 + hard_rules/validator 阈值 + 配置审计 | ✅ |

---

## 三、最大项一览（须重点排期 / 风险确认）

> 以下 9 项为全 PRD 工作量 / 风险最大、必须在排期与甲方前提确认中优先对齐：

1. **金蝶推送 outbox + 适配器**（07b）— **接口前提已确认（A-1：金蝶 OpenAPI 开放、Save/Submit/Audit 齐全）**，不再阻塞；降级为真实接入排期（凭据 + 6 公司组织码 A-7 + 账套字段编码）；全 PRD 第一大项。
2. **标签 / 单据模板引擎**（09）— 二维码拼接可配 + 渲条码 + 派生字段 + XSS 管控。
3. **编号规则月度重置 + 连号**（09）— 连号=金蝶幂等键，并发撞号风险。
4. **多公司单用户切换**（01）— 决策 B 已定，会话契约层改动。
5. **报表 / 看板 / 跨公司汇总非元数据化**（08）— 手写路由 + 手写聚合防火墙裁剪。
6. **扫码枪 / OCR 采集**（03）— 前端增强 + 手填兜底。
7. **物流 API（顺丰）**（06）— **「接好等配置」**：集成成形可运行，仅凭据占位、开关默认 OFF；前提=API 使用权购入即填凭据启用。
8. **边级角色推进路径补齐 FR-2.7**（00b 等）— 引擎自身欠账，未补前靠节点级角色 + commit_card 缓解。
9. **会话安全生产化**（01）— 引擎自身欠账，上生产前必整改。
