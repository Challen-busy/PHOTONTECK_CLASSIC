# 待甲方确认清单（全 PRD gaps 去重分组汇总）

> 本表把 00 / 00b / 01~09 各模块 `gaps` 去重，分 A~G 七组汇总，供甲方逐项拍板。每项标注来源模块。
> 决策 A（财务行级范围 + FINANCE_DIRECTOR）、决策 B（多公司绑定·各家角色相同）已落位，**原 01 模块 GAP-01/02/03 已关闭**（见组 E 说明）。
> **2026-06-16 甲方反馈批次又关闭多项**（见组 E 增补）：Q7（互检可同一人）、Q15（毛利率两套口径）、Q18（报价利润点/毛利对 SALES+SA 同层可见）、特批发货=做、售后工单=做、委外加工入库=做薄、顺丰=接好等配置、6 公司样本已自编、标签源已定位（仅余逐家签字）。
> **2026-06-16 follow-up 批次再关闭/默认确认（见组 E.3）**：**A-1 金蝶 OpenAPI 开放已确认**（接口层面可推、推送逻辑已写定，仅余真实接入）、**标签/单据现有规格 OK**（C-1/C-2/C-8 仅余逐家字段签字）、**提成 F-5 空页面占位已交付**（规则公式仍真待）、**顺丰 A-12 做空集成+UI 可见已交付**（仅待购买配置）、**KPI F-1 / 采购审批 B-1 按默认实现**（甲方确认）、Forecast G-1 轻量手录留痕已落地（直连后期）。
> **仍真待甲方的核心项**：①提成规则公式 F-5；②各客户标签/单据**首批字段来源逐家最终签字** C-1/C-2/C-8；③6 公司真实清单 + **各自金蝶组织码** A-7；④金蝶各账套固定字段编码 A-5/A-6；⑤Forecast 是否后期直连 G-1。
> 日期：2026-06-16。状态：待甲方评审。

---

## A. 金蝶对接前提（最高优先级，决定多个模块能否落地）

| # | 待确认 | 来源模块 | 默认假设 |
|---|---|---|---|
| ~~A-1~~ | ~~**金蝶云星空是否开放各业务对象 Save/Submit/Audit 接收接口**~~ **→ 已确认（甲方 2026-06-16，见组 E.3）**：金蝶云星空开放 OpenAPI（appid / 应用 id 鉴权），各业务对象 Save/Submit/Audit 写入接口齐全，**接口层面推送可行、推送整章可落地**；推送逻辑已写定，**仅余真实接入**（凭据 + 6 公司组织码 A-7 + 账套字段编码 A-5/A-6）后排真实对接 | 07b / 07a / 00 §6 | **已确认可接收**；剩余真实接入项见 A-5/A-6/A-7/A-8/A-9 |
| A-2 | 金蝶是否接收「预收/预付到账确认」「CN 冲销发票」单做账 | 07a / 07b | 默认接收（可能仅 Save 不 Audit）|
| A-3 | 推送链路推到哪一步（Save / Submit / Audit，是否直接生成凭证）按单据类型逐一定 | 07b | 默认 Save→Submit→Audit 到已审核 |
| A-4 | 采购订单 `pm_purorderbill` add 接口**明细子表字段**离线文档未展开（gap A）| 07b | 用采购入库/应付单同名字段类推，实施时查金蝶元数据接口 |
| A-5 | 销项/进项发票推应收/应付单的**单据类型 `billtype_number`/款项性质 `payproperty_number`** 具体编码 | 07b | 甲方/金蝶账套提供 |
| A-6 | 各库存单 `biztype_number`/`invscheme_number`/`linetype_number`/货主保管者类型等固定值 | 07b | 按甲方金蝶账套配置（8 配置）|
| A-7 | **6 公司具体清单 + 各自金蝶组织映射码**（company_id→org）| 01/02/06/07b/00 | 待甲方给清单 |
| A-8 | 物料/供应商/客户/仓库/单位**主数据编码是否与金蝶账套统一编码**（不统一则需映射表）| 07b/02 | 假设统一；不统一则新增映射维护 |
| A-9 | `cas_paybill`/`cas_recbill` 用 `_id` 内码——接受预先 query 换码 vs 金蝶提供按编码接口 | 07b | 默认预先 query 换码（多一跳）|
| A-10 | 实际回款日/金额来源：金蝶能否即时回填 vs 财务在本系统手录确认值 | 07a | 金蝶拉取优先、缺则财务手录 |
| A-11 | 金蝶往来单位/物料档是否需主数据级同步 | 02 | 默认随首张业务单带抬头/物料 |
| ~~A-12~~ | ~~**顺丰 API 使用权何时购**~~ **→ 设计已关闭（甲方 2026-06-16）**：做**空集成 + UI 可见**（06-5 模型/适配器/前端/运单号联动全部成形可运行、顺丰货物进度时间轴在前端可见），开关 `SF_INTEGRATION_ENABLED` 默认 OFF + 手填兜底；**仅余外部依赖**=顺丰 API 使用权购买后给 appKey/secret/customerCode 即开即用（甲方愿买未买，纯采购动作，非设计待定）| 06 | 集成成形+UI 可见已交付；待购买配置即启用，不阻塞 |
| A-13 | 调拨单（`im_transdirbill`）是否需推金蝶（公司内移库）| 03b/07b | 默认否；若金蝶按仓核算则需推 |
| A-14 | 金蝶错误码 → 中文提示映射字典是否需要 | 07b | 默认透传金蝶原文 |
| A-15 | 是否放开 PA 重推本产线采购单（默认仅 FINANCE/ADMIN 重推）| 07b | 默认仅 FINANCE/ADMIN |

## B. 流程关卡与审批（关卡角色 / 阈值 / 会签语义）

| # | 待确认 | 来源模块 | 默认假设 |
|---|---|---|---|
| B-1 | **采购审批是否分额度**（小额 PM/PD vs 大额财务/BOSS）| 04a | **甲方 2026-06-16：按默认实现**——采购统一财务审批；额度分级（备货 ≥20 万会审除外）暂不做，**如需分级阈值上线前再配**（节点级 allowed_roles + 边级 hard_rules 可加，非阻塞）|
| B-2 | **备货阈值 20 万的币种口径**（RMB/USD/HKD 各自 20 万 vs 折算统一）| 04b | 默认签单公司本币 20 万，跨币折算待定 |
| B-3 | **备货会审会签角色全集 + 串/并行**（除 PM/PD+财务外是否含销售总监/采购经理/BOSS）| 04b/00b | 默认 PM/PD + FINANCE **串行**会签（规避 D-02e）|
| B-4 | **会审语义**：多人全签 vs 升级到更高角色单签 | 09/00b | 默认串行两签（多人全签拆串联节点）|
| ~~B-5~~ | ~~出库两道关「仓库互检」复核人是否须 ≠ 拣货人（职责分离）~~ **→ 已由甲方 Q7（2026-06-16）关闭：互检可同一人复核（效率优先），系统不强制不同人、仅留复核痕**（见组 E）| 00b/03b | 已定：不实现「复核人≠制单人」校验 |
| B-6 | 入库审核是否需多级（访谈一闪"多级审查"）| 03a | 默认单级 PA |
| B-7 | read-privileged（BOSS/财务总监）的"待审"是否会污染待办；审批中心是否对其只渲染只读旁观 | 00b | 默认对 read-privileged 隐藏写动作（决策 A 一致性）|
| B-8 | 权限变更是否走系统内审批流 vs 线下/工单 ADMIN 直接操作 | 01 | 默认 ADMIN 直接 CRUD，无审批流 |
| B-9 | 边级角色是否为关卡硬需求（决定是否触发引擎 FR-2.7 补齐）| 09/全模块 | 默认优先用节点级 allowed_roles 规避 |
| B-10 | 是否报价门控是否需 PM 之上二次审（如老总）| 05a | 默认 PM 终决 |
| B-11 | SO 双签合同附件是否强制必填才能审核 | 05b | 默认建议必填（旧 ERP 不强制）|
| B-12 | RMA 核料自动判定 vs 人工（是否我方卖/过保）| 04b | 默认 effect 自动倒查给建议、PA 确认 |
| B-13 | 报关申报数量是否硬校验「≤实际进货数」 | 06 | 默认硬校验（与现行实现取向不同）|
| B-14 | 受管制行未挂证是否硬卡申报 vs 软留痕 | 06 | 默认软留痕（升级硬卡涉合规责任）|

## C. 模板 / 编号 / 字段来源（配置域）

| # | 待确认 | 来源模块 | 默认假设 |
|---|---|---|---|
| C-1 | **各客户标签/单据规格"待确认"字段来源逐一签字**（~21 客户 × 多标签类型）| 09/02/03/00 | **甲方 2026-06-16：现有规格（灵活模板）OK**——09 标签/单据规格册结构 + 客户差异矩阵已确认可用；**仅余各客户首批字段来源逐家最终签字**（业务部门确认现有规格映射的真实字段值，纯数据确认非设计待定，仍真待）|
| C-2 | 二维码拼接细则（各客户分隔符 `& ; + , _` + 字段顺序）首批数据 | 09 | **规格机制已确认 OK**；仅余各家分隔符+字段序首批真实值逐家签字（仍真待，随 C-1）|
| C-3 | 标签派生字段表达式语法（如"合同号前5位"=供应商代码）| 09 | 待定 |
| C-4 | 打印驱动集成方式（现状 BarTender + Excel 数据源）| 09 | 待甲方；渲染产物先行 |
| C-5 | 编号规则：抬头编码映射表来源 + 上线期初 current_seq 起点 | 09/03a | 待甲方 |
| C-6 | 销售订单内部订单号统一口径（`{YYYYMM}-{seq}` 月度连号 vs `FT-{YYMMDD}-{seq}`，两种并存）| 05b/09 | 默认月度连号（用于统计当月签单）|
| C-7 | 发票号 I 与 PL 同尾号配对的精确联动规则 | 05b | 待甲方 |
| C-8 | 各公司 invoice/合同/送货单模板首批字段来源逐一签字 | 05b/09 | **规格机制已确认 OK**（09 单据模板规格册）；仅余各公司首批字段来源逐家签字（仍真待，随 C-1）|
| C-9 | 单据回签流转颗粒度（系统内追踪签回 vs 邮件附件即可）| 09/05b | 默认模板标志 + 附件占位 |
| C-10 | 一签宝是否提供 API（合同/对账电子签）| 05b/07a | 默认占位 + 人工上传盖章 PDF |
| C-11 | 客户/产品是否需建档审核态机 | 02 | 默认轻量两态或纯字典，无财务关卡 |

## D. 多公司 / 会话 / 数据范围（决策 A/B 落地细节）

| # | 待确认 | 来源模块 | 默认假设 / 已定 |
|---|---|---|---|
| D-1 | **多公司切换的会话契约实现方式**（"是否做"已由决策 B 拍板）：会话载 `active_company_id` 重签 Cookie vs 每请求带公司头 | 01/00b | 技术选型，默认重签 Cookie；二选一待技术+甲方定 |
| D-2 | 是否需要"立即踢人"+ 口令/会话生产化整改优先级（bcrypt+每用户盐 + 服务端会话吊销）| 01 | 默认惰性吊销（非实时踢人）；上生产前整改 |
| D-3 | 临时代管授权颗粒度（休假代拉看同组哪些人数据）| 01/04a | 默认按产线组 + `valid_until` 软到期 |
| ~~D-4~~ | ~~利润点对客户侧 SA / 对内的可见性细则~~ **→ 已由甲方 Q18（2026-06-16）关闭：报价利润点/毛利对 SALES+SA 同层可见（报价决策用）、采购买价对销售端 SALES+SA 一并隐藏**（见组 E、00 §8）| 01/05a/08/00 | 已定：墙体=采购买价，非利润点 |
| D-4b | **卖价对客户侧**的可见性细则（Q18 只定了对内；对外客户能看到哪些价仍待定）| 05a/05b/09 | 默认客户只见报价单含税卖价，不见内部利润点/成本 |
| D-5 | **报价利润点/毛利对 SALES+SA 已可见（Q18）**；但 08 报表的**毛利率聚合列**对销售是否同样放行（隐藏成本仍可能由毛利率反推买价）| 08 | Q18 后报价侧毛利对 SALES+SA 可见；报表侧聚合毛利列默认仍按字段防火墙保守（GAP-08-1：是否放行须甲方确认反推风险可接受）|
| D-6 | 审计四本的保留期 / 归档策略 | 01 | 源中未提及，待甲方 |
| D-7 | HK/内地以外是否还有第三类区域配置 | 01/06 | 默认仅两类（HK/内地）|
| D-8 | 销售经理是否独立角色 vs SALES+管理范围（线索分派）| 05a | 默认 SALES + 管理范围 |
| D-9 | 是否仅 BOSS/财务总监需跨公司只读，还是部分审计岗也需要 | 01/08 | 决策 A 收敛为 {BOSS, FINANCE_DIRECTOR}；如有审计岗再扩 privileged 成员 |
| D-10 | 财务总监财务汇总取本系统视图聚合 vs 拉金蝶跨组织报表；BOSS 与财务总监指标边界 | 08/07a | 默认拉金蝶已确定视图；指标边界待分工 |

## E. 已由决策 / 甲方反馈关闭的项（不再待定，留痕）

### E.1 决策 A/B 关闭（2026-06-16 前）
| 原 gap | 关闭依据 |
|---|---|
| 01 GAP-01（财务行级范围是本公司账套 vs 看全部）| **决策 A**：财务=本公司账套；`FINANCE` 移出行级 privileged；新增 `FINANCE_DIRECTOR` 跨公司只读 |
| 01 GAP-02（跨公司只读是否仅 BOSS）| **决策 A**：跨公司只读 = {BOSS, FINANCE_DIRECTOR}；普通 FINANCE 本公司账套 |
| 01 GAP-03（是否真有一人多公司、每公司不同角色）| **决策 B**：确有一人多公司，**各家角色相同**，由管理员可配置开通哪些公司（无 per-company 角色）|

### E.2 甲方 2026-06-16 反馈批次关闭
| 原 gap | 关闭依据 |
|---|---|
| 互检复核人是否须 ≠ 拣货人（原 B-5）| **甲方 Q7**：互检**可同一人**复核（效率优先），系统不强制不同人、仅留复核动作痕；原拟 `created_by_id != user` validator **不做**（03b §页面4 / engineFlags 已移出）|
| 毛利率口径分母是成单额 vs 交付额（原 08 GAP）| **甲方 Q15**：毛利率做**两套**，分母分别=成单额 与 实际交付额，并列/可切换；共用同一毛利额分子（08 §KPI 口径表）|
| 报价利润点/毛利对内可见性（原 D-4：SA 可见、SALES 不可见）| **甲方 Q18**：报价利润点/毛利对 **SALES+SA 同层可见**（报价决策用）；**采购买价（询价价/PO 单价/入库成本/在途单价）对销售端 SALES+SA 一并隐藏**——防火墙墙体=采购买价非利润点（00 §8、04a、05a 已一致）|
| 特批发货事后补单是否做（原 G-2）| **甲方拍板做**：05b 页面 4b「特批发货（先发后补单）」**可隐藏模块**（`feature.special_batch_shipment` 默认 OFF）+ ★财务特批审 + 强制事后补录 SO 勾稽 + 两段式推金蝶；正常发货「必关联 SO」零容忍不变 |
| 售后技术工单是否做（原 G-4 部分）| **甲方拍板做**：05c「售后技术工单」轻量 doc_type `SERVICE_TICKET`（登记+归属+留痕+状态+统计），FAE 主处理，判定退换→派生 RMA；**不做**复杂派工/工时计费/备件/SLA 硬卡 |
| 委外加工入库是否做（原 G-4 部分）| **甲方拍板做薄**：03a-9 委外=一发一收（委外发料[出库]+委外加工入库[入库]），**不建工序/BOM/工时/领料倒冲**；成本核算仍在金蝶；区别于 K3 原工序版（不照搬）|
| 顺丰货物进度集成性质（原纯占位⑪）| **甲方：顺丰愿买未买，先做好等配置**——06-5 升级为**「接好等配置」**：模型/适配器命令/前端/运单号联动全部成形可运行，仅鉴权凭据 + base_url 留空占位，开关默认 OFF |
| 6 公司样本（原"待甲方给清单"占位）| 已在各模块**自编 6 公司样本**（HK/内地分布）支撑设计；**仅余实际公司清单 + 金蝶组织映射码**待甲方提供（仍见 A-7）|
| 各客户标签源是否定位（原"无来源"）| 标签源已定位（各客户 Python 标签程序 + BarTender + 出库往来邮件 PL/INV/送货单样本，已做灵活模板）；**仅余各客户字段来源逐家签字**（仍见 C-1/C-2/C-8）|

### E.3 甲方 2026-06-16 follow-up 批次关闭 / 默认确认
| 原 gap | 关闭依据 |
|---|---|
| **A-1 金蝶是否开放业务单据接收接口**（原最高优先级阻塞前提）| **甲方已确认**：金蝶云星空开放 **OpenAPI（appid / 应用 id 鉴权）**，各业务对象 **保存(Save)/提交(Submit)/审核(Audit)** 写入接口齐全——**业务单据推送在接口层面可行**，推送整章（07b outbox + 适配器 + formId/字段映射）**可落地**、推送逻辑已写定；**剩余仅真实接入**（金蝶凭据 appid/appSecret + 6 公司组织码 A-7 + 各账套固定字段编码 A-5/A-6）后排真实对接——**前提不再阻塞，降级为真实接入排期项**（00 §6、07b 已落档）|
| 标签 / 单据现有规格是否 OK（原 C-1/C-2/C-8 全待）| **甲方确认现有规格 OK**：09 标签/单据规格册（灵活模板 + 客户差异矩阵 + 二维码拼接机制）确认可用；**仅余各客户/各公司首批字段来源逐家最终签字**（纯数据确认，仍见 C-1/C-2/C-8）|
| 提成 F-5 是否先交付占位（原"规则签字前不实现"）| **空页面占位已交付**（08 提成卡 + 09 `commission_rule` 占位结构）；**提成规则公式甲方未给出 = 仍真待提供**，规则到位后再实现计算 command（仍见 F-5）|
| 顺丰 A-12 集成性质收口（原"何时购"待定）| **设计已关闭**：做**空集成 + UI 可见**（模型/适配器/前端/运单号联动成形 + 货物进度时间轴前端可见），开关默认 OFF + 手填兜底；**仅余外部采购动作**=顺丰 API 使用权购买后填凭据即启用（非设计待定，仍见 A-12）|
| KPI F-1 / 采购审批 B-1 口径（原逐项待签）| **甲方确认按默认实现**：KPI 先按默认口径（成单按下单月、毛利率两套口径 Q15）、采购统一财务审批（不分额度，备货 ≥20 万会审除外）；上线前可微调，非阻塞（仍见 F-1/B-1）|
| 客户 Forecast 接单（原 G-1 占位）| **已落地轻量手录留痕**：05d 升格独立子模块 + 两张轻量单据 + 联动备货/转 SO；**首期不直连**客户异构系统；**直连列后期 ➕扩展**（开关默认 OFF，是否做待甲方，仍见 G-1）|

## F. 业务口径 / KPI / 字典（统计与维度）

| # | 待确认 | 来源模块 | 默认假设 |
|---|---|---|---|
| F-1 | **6 个 KPI 精确公式**（分母分子、时间归属：成单按下单月 vs 发货月；毛利率分母用成单额 vs 交付额）| 08 | **甲方 2026-06-16：先按默认口径实现**（成单按下单月、毛利率两套口径见 Q15）；指标名+默认语义已落 08，**精确公式上线前可微调**（保留为可校准项，非阻塞）|
| F-2 | 事业部（光通信/科研）+ 科研 7~8 细分市场主数据来源（customer.事业部+订单memo vs 独立维度表）| 08/05a/02 | 待与主数据对齐 |
| F-3 | 「咨询量」计量单位（线索数 vs 商机进入数 vs 咨询次数）| 08 | 待甲方 |
| F-4 | 业绩目标设定主体/周期粒度（谁定、月季年、是否分产线分客户）| 08 | 待甲方 |
| F-5 | **提成规则全部细则**（基数=成单/交付/毛利、比例/阶梯/封顶/扣减、周期、团队拆分）| 08/09/00 | **空页面占位已交付**（08 提成卡 + 09 `commission_rule` 占位结构）；**规则公式甲方未给出 = 仍真待提供**——规则到位前提成只占位不算（核心未知，签字后再实现计算 command）|
| F-6 | 应收「超额」阈值口径（信用额度硬阈值 vs 参考、是否需财务确认动作）| 08/07a | 默认仅提醒、不硬拦 |
| F-7 | 各事件提前提醒天数/重复频率/邮件模板；是否需短信/微信/桌面推送 | 00b | 默认仅站内+邮件 |
| F-8 | 应收销售「本人客户」过滤字段（AR 是否带 owner_sales_id）| 08/07a | 待与模块 5/2 确认 |
| F-9 | 退运 180 起算日（放行日+180 vs 海关批文日+180）| 06 | 默认放行日+180，待实际海关口径 |
| F-10 | 备货消单匹配口径（型号+意向客户 vs 仅型号）；超期提醒阈值与对象 | 04b | 默认型号+意向客户；挂 30 天起提醒 PM+发起人 |
| F-11 | 样品超期天数基准日（寄客户日/应归还日/申请日）| 04b | 默认寄客户日 |
| F-12 | 进项发票审核财务核对项清单（是否含税/汇率）| 04a | 默认核发票号/金额/与入库实收一致 |
| F-13 | 采购在途提醒触发阈值（超期几天、对谁推）| 04a | 默认承诺 ETA 过期且未发货即提醒 PA |
| F-14 | 控制模式是否仅 SN/LOT 两值；同型号多供应商 HS/ECCN/COO 随供应商 vs 随批次 | 02 | 默认仅两值；默认随产品代码/入库批次 |

## G. 范围待定 / 占位（决策⑫ 默认占位或排除，待甲方拍板纳入）

| # | 待确认 | 来源模块 | 默认假设 |
|---|---|---|---|
| G-1 | **客户 Forecast 接单**是否纳入系统范围 / 颗粒度 / 是否直连客户供应商系统（异构，烽火多系统）| 05b / **05d** | **已落地（轻量手录留痕）**：05d 升格为独立子模块 + 两张轻量单据（`customer_forecast` 滚动预测 + `forecast_acceptance` 接单应答）+ 联动备货(04b)/转 SO(05b)；**首期不直连**客户异构系统（手录抄写）；**仍真待**：①颗粒度/是否纳入首期由甲方拍板；②**直连客户供应商系统列为后期 ➕扩展**（开关 `CUSTOMER_PORTAL_SYNC_ENABLED` 默认 OFF），是否做、做哪些客户待甲方 |
| ~~G-2~~ | ~~特批发货事后补单是否做~~ **→ 甲方已拍板做**（05b 页面 4b 可隐藏模块 + ★财务特批审 + 强制补单勾稽，见组 E.2）；**仅余**：特批默认隐藏的 per-company 启用策略 + 特批出库金蝶记账口径细则待甲方 | 05b | 已落地；记账口径细则随 A-1/A-2 |
| G-3 | 标书 / 认证颗粒度与是否纳入首期 | 05a | 默认轻量占位 |
| ~~G-4~~ | ~~售后技术工单 / 委外加工入库是否做~~ **→ 甲方已拍板：售后工单=做（轻量 05c）、委外加工入库=做薄（03a-9 一发一收）**（见组 E.2）；**仍排除**："带货走流程"性质货（默认不调整入账）| 03/04/05c | 已落地（薄/轻量）；"带货"仍排除 |
| G-5 | AI 助手 / Agent 对话面 | 00b/05a | **明确不做**（甲方要确定性传统工具），前端 Agent 面隐藏 |
| G-6 | PCN 样品限售是否进系统拦截 | 04b | 默认人工筛选+标记、不自动拦截 |
| G-7 | RMA Credit Note/退款财务联动颗粒度；内部消化库存损失记账口径 | 04b/07a | 本系统只标记/触发，做账损益在金蝶 |
| G-8 | 库位平面图可视化选位；6 公司库位编码体系是否统一；内地库位 | 02/03a | 默认纯编号下拉，平面图占位 |
| G-9 | 报关任务自动派生（跨境单据自动预判需报关）；委托资料包对象 vs 纯清单导出 | 06 | 默认不自动派、物流主任顺手建；默认纯清单 |
| G-10 | 报关行/自报具体单证流程细节 | 06/00 | 蓝图 §4 待确认 |
| G-11 | 邮件外发（进库通知/发货通知/对账/资料清单）保留 vs 仅系统内通知；邮件服务器/发件域 | 03a/05b/06 | 默认保留邮件（甲方现流程强依赖），配置待甲方 |
